import React from 'react';
import {SafeAreaView} from 'react-native';
import MyInfo from '../../views/exam/MyInfo';

const MyInfoPage = () => {
  return (
    <SafeAreaView style={{flex: 1}}>
      <MyInfo />
    </SafeAreaView>
  );
};

export default MyInfoPage;
