import React from 'react';
import {SafeAreaView} from 'react-native';
import OxMain from '../../views/ox/OxMain';

const OxMainPage = () => {
  return (
    <SafeAreaView style={{flex: 1}}>
      <OxMain />
    </SafeAreaView>
  );
};

export default OxMainPage;
